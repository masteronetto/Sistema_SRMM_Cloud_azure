# Componentes legacy

Los componentes de esta carpeta pertenecen a la implementación anterior y ya no son entradas de la aplicación React.

La implementación activa está en `frontend/src/`. Estos archivos se conservarán temporalmente para comparar funcionalidad durante la migración de reportes, historial y permisos. Se eliminarán cuando la paridad funcional esté verificada.
